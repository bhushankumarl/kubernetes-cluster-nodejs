apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodejs-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: node-sample-1
  template:
    metadata:
      labels:
        app: nodejs
        app.kubernetes.io/name: node-sample-1
    spec:
      containers:
        - name: node-sample
          image: node-server
          ports:
            - containerPort: 3000
